# CLI reference

Full reference for the `portage` command, shipped by the `portage-cli` gem — every
subcommand, flag, and environment variable it reads, single-sourced from the gem's own
README.

{% include-markdown "../portage-cli/README.md" %}
