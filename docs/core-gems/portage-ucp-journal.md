# portage-ucp-journal

The buyer-side purchase journal, plus the injectable `Store` abstraction it's built on —
backs `portage history` in the CLI.

{% include-markdown "../../portage-ucp-journal/README.md" %}
