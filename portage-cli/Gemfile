source "https://rubygems.org"

gemspec

gem "portage-ucp", path: "../portage-ucp"
gem "portage-ucp-client", path: "../portage-ucp-client"
# Optional at runtime (not in the gemspec), but in the dev bundle so the
# suite covers the confidence gate. confidence_check_spec stubs it out for
# the not-installed path.
gem "portage-ucp-decision", path: "../portage-ucp-decision"
gem "portage-ucp-journal", path: "../portage-ucp-journal"
# Optional at runtime (not in the gemspec), but in the dev bundle so the
# suite covers Buy#webmcp_flow. buy_spec.rb stubs Webmcp.available? for the
# not-installed path.
gem "portage-ucp-webmcp", path: "../portage-ucp-webmcp"
